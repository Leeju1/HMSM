{
  "dataset": "lamp_return",
  "N": 126,
  "hidden_dim": 64,
  "out_dim": 32,
  "alpha": 0.6,
  "beta": 0.3,
  "final_epochs": 145,
  "epoch_selection": "fixed; paper value is the repeated-CV mean validation-loss argmin",
  "pm_dir": "outputs/lamp_return/graphs/posterior_mean",
  "graphs_dir": "outputs/lamp_return/graphs/draws",
  "model_dir": "outputs/lamp_return/embedding/model",
  "emb_dir": "outputs/lamp_return/embedding/draws",
  "validation_dir": "outputs/lamp_return/embedding/validation",
  "tuning_dir": "outputs/lamp_return/embedding/tuning",
  "grid": {
    "d_grid": [
      16,
      32,
      64
    ],
    "per_d_config": {
      "16": {
        "hidden_dim": 32,
        "lr": 0.01,
        "epochs": 1000
      },
      "32": {
        "hidden_dim": 64,
        "lr": 0.01,
        "epochs": 1000
      },
      "64": {
        "hidden_dim": 128,
        "lr": 0.01,
        "epochs": 1000
      }
    }
  }
}
